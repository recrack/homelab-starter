# Maintenance modules

Maintenance modules keep hosts healthy without making the whole starter kit
depend on one operating system or package manager.

Current module:

- `maintenance/mole` — Mole cleanup and update scheduling on macOS.
